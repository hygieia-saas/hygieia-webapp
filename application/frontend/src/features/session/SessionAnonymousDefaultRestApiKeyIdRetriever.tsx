import React from 'react';

const SessionAnonymousDefaultRestApiKeyIdRetriever = (): JSX.Element => {
    return <>Need to get a default REST API key id for an anonymous user...</>;
};

export default SessionAnonymousDefaultRestApiKeyIdRetriever;
